/**
 * The npm command `start:app` or `build:app` must have
 * been run for these files to exist.
 */
import '../dist/app/main-es2015';
import '../dist/app/polyfills-es2015';
import '../dist/app/runtime-es2015';
import '../dist/app/vendor-es2015';